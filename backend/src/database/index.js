export { connectDB, closeDB } from './connection.js';

// Base Schema Definitions or Global Plugins can be exported here if needed later
// Example: export { default as BaseSchema } from './BaseSchema.js';
